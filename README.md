# my-first-mud
A simple MUD game to learn Go

## Project Layout

- `client/` - Phaser 4 client built with Vite and TypeScript.
- `server/` - Go server module.

## Getting Started

Client:

```sh
cd client
npm install
npm run dev
```

Server:

```sh
cd server
go run ./cmd/server
```
